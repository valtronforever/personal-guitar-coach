# Compound and odd meter — local review

2026-09-17. Same-agent Domain/Learning/transport/notation/content review. Scope: 6/8, 12/8, 5/4, 7/8 and curriculum topics 68/71. Full curriculum remains in progress.

## Contract and findings

- TimeSignature now separates written numerator/denominator from pulse ticks/count. Historical 3/4 and 4/4 keep quarter BPM; 5/4 also uses quarters, 6/8 and 12/8 dotted quarters, 7/8 eighths. PPQ stays 960. Shared conversion reaches transport/sample scheduling, session limits, expected attacks, sustain, bends and capability. No hidden quarter-equivalent BPM, tempo map, second capture pipeline or DSP change was added.
- Optional positive beatGrouping values sum to the pulse count. Odd defaults are 3+2 and 2+2+3; explicit alternatives persist in snapshots. Group starts accent the click; guitar accent metadata is separate. Old absent fields keep canonical encoding. Gapped metronome patterns validate on the actual pulse grid. Grouped/new-meter event materials retain whole bars to avoid silently resetting grouping phase; ordinary quarter-meter omission fragments retain their whole-pulse rule.
- Independent 44.1/48 kHz ×60/137 BPM sample goldens exposed an existing count-in display rounding mismatch: at a rounded click frame it could still report the previous beat. The indicator now uses the same half-sample convention as scheduled onsets and musical position. Audio click samples and historical timing are unchanged.
- Staff prints the real denominator, beams compound thirds/odd groups and keeps ordinary simple-meter eighth pairs. TAB, hit testing, following, compact count-in, spoken beat positions and bend graph use the pulse unit. Tempo labels in preview/practice/results explicitly name the unit; new grouped/meter coach requests use file-coach-4. Historical ordinary/bend/omission versions retain their meanings.
- Full App regression caught an overflow in the new notation-boundary helper for a valid extreme display timeline ending at Int64.max. It initially extrapolated the end of the final partial bar. The helper now bounds relative offsets by the actual remaining duration before adding them, and rejects out-of-range bar requests. The existing giant-timeline regression was expanded to every supported signature; silent-beat lookup also avoids overflowing an absolute bar end.
- Two substantive bilingual lessons supply seven progressive exercises, quizzes and self-observation. Compound meter compares four 6/8 bars with identical musical events in two 12/8 bars, including one sustained dotted-quarter tonic. Odd meter compares unchanged pitch/time motifs under 3+2/2+3 and 2+2+3/3+2+2 emphasis. All counts, tempo units and returns are explained; accent strength and internal counting are not claimed as audio-verified skills.

## Verification

- Targeted Domain/Learning/Audio checks pass: pulse/count/denominator goldens, invalid groups/omission grids, preserved default JSON, quarter-vs-dotted duration capability, exact grouped click samples, count-in, partial seek/repeat, chunk invariance, and compensated expected attacks including a deliberate +150 ms error. All historical transport checks pass after the count-in fix.
- Fourteen targeted App tests pass (0.910 s), including compound/odd beams, a held note split at the dotted pulse, old staff/triplet behavior and metronome recording/prompt checks. The initial full App run later trapped on the extreme timeline described above. After the bounded-offset fix, all 149 App tests pass in 152.242 s. A final 13-test timeline/metronome/render run passes in 1.930 s after the two-row practice tempo layout and new-meter prompt assertion; all 16 exports are byte-identical to the earlier render set.
- Sixteen offline exports cover all four new signatures in English/dark and Ukrainian/light, staff and compact TAB. Inspected 6/8, 12/8 and 7/8 beams, 5/4 grouping, two/seven-count introductions, final dotted-quarter note and compact spacing. Representative inspected images are retained in docs/reviews/compound-odd-meter/. Export ink checks reject an empty/partial staff. Native window interaction/VoiceOver is not established by these images.
- All seven activities are checked across eight tunings ×five fret counts against independent pitches/onsets/durations, bar count, pulse, grouping, final hold and tempo limits. Content validation accepts 75 bilingual bundles with zero issues. Inventory is 68 authored, 1 needing review, 59 todo; module 65–72 is authored, not hardware-accepted. Project, 840 UI keys, four Python author-tool tests, partial inventory and UI-source type-check pass.

## Remaining acceptance

All 269 Core tests pass explicitly serially: 23 Persistence (0.054 s), 104 Learning (82.484 s), 42 Domain (0.017 s), 94 Audio (286.629 s), 6 AgentBridge (1.604 s). Signed local Release/archive validates 75 bundles /226 YAML files (`docs/benchmarks/compound-odd-meter-release-bundle.json`). Binary SHA256 `237ae665385163fbaa4f9a935063cff012a8d647edb55ea4633cbceb1779be97`; archive SHA256 `f3e8defdb66a6b92501257742e59f265f28b5f0a95c57ebd0122ab098462670b`. The signed sandbox-to-separate-XPC invalid-request probe passes with no provider call. Native launch is not verified; exact-head CI remains pending. Native/hardware acceptance remains pending_user in USER-VALIDATION.md; no real instrument performance is inferred from synthetic schedule, assessment fixtures or offline images. Remaining lead articulations and later curriculum modules are still open.

### CI compiler follow-up

The first exact-head CI run (35170242764) failed during compilation: its Swift compiler could not type-check the combined synthetic attack-onset expression in `MeterAssessmentTests` within its expression budget. Split expected onset, calibration offset and final-note error into explicit Double values and typed the attack array. No expectations or tolerances changed. The local targeted MeterAssessmentTests run passes (1 test, 0.007 s); the new commit requires a fresh exact-head CI run. This is test-only, so the archived Release payload above remains applicable.

Exact-head CI 35170881942 passed on 27da78cf2b360359852e59e0120c9eba819d1fd9, including Core/App tests, audio regression, synthetic soak, signed Release/archive/XPC and CI build configurations. PR #79 merged. Native/hardware acceptance remains open.
