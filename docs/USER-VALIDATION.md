# Final user and hardware validation

The user requested that checks requiring their help take place **after all 22 implementation tasks**. These checks do not block software implementation or merging reviewed code. They remain open evidence requirements, not presumed successes. No distribution or notarization is planned.

## Pending checks

| ID | Related tasks | Needed evidence | State |
| --- | --- | --- | --- |
| U01 | 02, 11 | Electric guitar into the user's USB interface: input/channel identity, clean level, simultaneous headphone click, unplug/replug, 44.1/48 kHz where supported | Pending final user session |
| U02 | 12, 13, 17 | Actual guitar recordings/playing: low/high notes, repeated attacks, sustain, alternate tuning, pitch/onset accuracy and tuner behavior | Pending final user session |
| U03 | 15, 17 | Personal synchronization: two 16-note passes, explicit Apply and approximate early/late scoring; repeat with wired headphones and separate Bluetooth output, then after reconnect/sleep. Follow PERSONAL-SYNCHRONIZATION.md. Cable calibration is deferred; hardware latency accuracy is not claimed. | Pending final user session |
| U04 | 11, 12, 20 | Acoustic guitar through a microphone or piezo pickup: source selection, background noise and click leakage, reliable/uncertain feedback | Pending final user session |
| U05 | 18, 19, 20, 22 | Beginner walkthrough of lessons, accessibility, musical fingering/notation, and localized feedback with the user's playing | Pending final user session |
| U06 | 21 | Unseen real guitar chord recordings with labels for held-out polyphonic research validation | Pending final user session |
| U07 | 01, 04, 17, 18, 19, 20 | Complete/repair Xcode 26.6 system components: xcodebuild project commands fail loading IDESimulatorFoundation because /Library/Developer/PrivateFrameworks/DVTDownloads lacks a symbol. The standard runFirstLaunch process was stopped after remaining idle without output; no success claimed. Then run Xcode UI tests. Native SwiftPM .app builds and package tests remain usable. | Pending final environment validation |
| U08 | 02, 11, 13, 20 | Approve the app's normal macOS microphone permission dialog, then validate actual Scarlett input. The computer-use tool disallows UserNotificationCenter; this restriction was not bypassed. The input permission flow was left unconfirmed and the probe closed. | Pending final user session |
| U09 | 14, 15, 16, 20 | Audible click/example on the selected USB headphone channel at several BPM; 15-minute measured output timing and UI-load run; note/cursor alignment, loop boundaries, and input click leakage. Task 14 native built-in output was exercised silently; this does not establish audibility or round-trip latency. | Pending final user session |
| U10 | 16, 17, 18, 19, 20 | Full physical practice: lesson handoff, tuning confirmation and clean signal preflight, count-in alignment, first/last attacks, healthy all-missed attempt, pause/seek/tempo/retry, repeated independent attempts over 15 minutes, unplug/sleep during final drain, bounded memory and no cross-attempt evidence. Repeat includes a finalization pause and new count-in. Verify electric DI first, then microphone/piezo. | Pending final user session |

Task 02 discovery (2026-09-09): Scarlett 2i2 USB was connected with 2 inputs, 2 outputs, 44,100 Hz and a 512-frame buffer. Native output click start/stop succeeds; audibility, input PCM and played-guitar evidence are still pending. Task 01 Xcode Debug/Release builds passed on a clean GitHub runner in PR #23; local Xcode UI tests still require U07.

Record the Mac/macOS, interface/driver, channel, actual sample rate/buffer, source type, procedure, observed result, and any fix/retest here. Do not record private audio without an explicit recording action. Synthetic and software-only checks belong in the individual task evidence and cannot close these hardware rows.

Task 09 adds independent UI-test source type checking (`Scripts/check_ui_sources.py`), now passing locally and included in CI. It verifies Swift/XCTest API compilation without loading the broken xcodebuild component; U07 still requires actual UI-test execution.

Task 11 discovery (2026-09-10): Scarlett is currently absent from the native input/output menus. MacBook Pro Microphone reports 48,000 Hz and 512 frames with master input-level control; MacBook Pro Speakers exposes two selectable output channels. Built-in, Teams virtual audio and MOMENTUM 4 are listed. No input capture, test click or hardware parameter change was attempted in this task. Shared settings/UID-channel persistence and en/uk dark/light UI were verified; test selections were cleared afterwards. U01/U04/U08 remain open, including physical output-channel mapping after the explicit-channel output change.


Task 12 software evidence: MPM/energy-flux analysis runs on the sole PCM worker; 71 core tests and a 3,360-case full offline benchmark pass. Public Iowa acoustic clips are development data with independent estimated annotations, not the user's recordings or proof of electric-interface capture. U02 additionally needs a held-out clean electric DI corpus/live playing, dominant harmonics/low notes, onset repeats and actual tuner settling; U04 needs room noise/piezo/click leakage. No input permission or capture was attempted for task 12.

Task 17 visual-check limitation (2026-09-10): the native computer-use connection returned “Sky Computer Use native pipe closed before response” after selecting the synthetic assessment fixture. The app process remained running; reconnecting and resetting the tool did not restore observation. The new assessment card has build/state/localization coverage, but its native visual/accessibility inspection is still pending. No microphone or system permission was requested. Repeat the en/uk and light/dark assessment-fixture inspection once the connection is available.

Task 18 adds results/history/recommendation flows to U05/U07/U10. Inspect both languages and themes, narrow-window wrapping, keyboard/VoiceOver TAB symbols and evidence disclosure, result → audio setup sheet handoff, saved tuning → tuner, repeated retry → fresh confirmation/count-in, and history language changes without score mutation. The native computer-use channel still returned the same pipe-closed error on reconnect; no visual success or executed UI-test result is claimed. Software tests cover thresholds, archive restoration, comparisons and navigation state.

Task 19 expands the course to six original en/uk lessons (see `docs/STARTER-COURSE.md`). U05/U07 must inspect all steps and long texts in both languages/themes, first-fret comfort, C-major and pentatonic string changes, and the Em shape versus separate-note practice distinction. U10 should include at least the repeated-note/rest rhythm exercise and Em arpeggio on the real guitar. Synthetic event fixtures validate targets, grading and saved retry links only; they do not establish live performance accuracy.

Task 20 adds the consolidated [product acceptance matrix and 15-minute physical protocol](VALIDATION.md). Signed local Release/ZIP and offline resources are verified; final offline launch, icon presentation, real-time CPU/RSS/clock drift and callback loss measurements remain U07/U09/U10. The accelerated 900-second synthetic-event benchmark does not close any physical gate. No Scarlett was connected during the task-20 discovery check.

Task 21 delivered a [reproducible chord feasibility study](research/21-chords.md) and no-go ADR. U06 still needs independent electric DI/real-distortion/microphone/piezo recordings and human note/strum labels. The twelve-recording public acoustic player-held-out subset and its simulated distortion fail integration gates and do not close those physical requirements. Product chord scoring remains disabled.

Task 22 adds the bounded Staff prototype to U05/U07: select each C-major/pentatonic event in Staff and TAB, check shared lesson/fretboard state, written-versus-sounding octave, key signs, pauses and durations, rapid arrow/Shift-Space movement across bars, horizontal cursor following, unsupported chord/tie explanation and both languages/themes at minimum window size. Forty-eight offline geometry renders and model tests pass; native reconnect still failed with the same pipe-closed error. These artifacts do not establish VoiceOver or full native interaction.

Final native follow-up (2026-09-10, after PR #44): selecting the Release app recovered CUA access. Actual open-string/C-major staff selection, cross-bar keyboard focus, TAB selection parity, en/uk changes, light/dark presentation, practice handoff without a route, idle tuner and empty history were observed. A displaced native staff focus ring was found, fixed and rechecked in the rebuilt Release. See the [bounded native review](reviews/22-native-ui-review.md) for exact actions and remaining gaps. The earlier pipe failures are historical evidence; the Debug synthetic-results action still reproduced the error, while Release observation recovered. This partially advances U05/U07; VoiceOver, results/retry, full course/minimum-window and Xcode UI tests remain open. Scarlett is still absent; no audio capture or physical gate was completed.

C Standard follow-up (2026-09-11): the user reports a connected interface/guitar tuned to C Standard. Read-only system audio discovery now lists Scarlett 2i2 USB with two input/output channels at 44,100 Hz. This establishes device availability only, not the selected input channel, permission, clean DI signal or audio accuracy. For U02/U10, select C Standard (6 → 1: C2 F2 B♭2 E♭3 G3 C4), manually confirm each tuner string and run the new C Standard open-string/rhythm/Cm practice variants. Include low C2 settling/attacks and distinguish automatic G3/C4 harmonic confirmation from an actual tuning failure. The 24 synthetic PCM rate/string/mode cases and 48 deterministic course-assessment cases do not close physical gates.

Bounded native C Standard evidence: [review](reviews/06-c-standard-review.md) records en/uk preset/target frequencies, A♭ step-to-fretboard/staff, preserved selection across language change and practice handoff. The idle tuner currently shows Scarlett Channel 1, but that is a UI setting, not proof of physical signal. C Standard remains selected; System language/appearance are preserved.


Automatic adaptation follow-up (2026-09-12): use the same six-lesson catalog after selecting C Standard, Drop D or a custom profile. U05/U10 should check teaching clarity and comfort of the generated fingerings, especially Drop D low-string changes and custom chord shapes. Verify the physical guitar with Tuner before a new attempt. Legacy recommendation retries intentionally retain the saved tuning. Software adaptation and synthetic attempt interruption do not establish new hardware accuracy.

Preset-family follow-up: confirm B Standard, Drop B♭ and Drop A low-string tuner feedback on real guitar input, including approaching A1 from below. Software/synthetic checks do not certify this hardware scenario. Software grading now includes A1–B1 within the 55 Hz frequency floor; this does not establish real-guitar accuracy.

Independent lesson text / authoring follow-up (2026-09-12), U05 pending: open “Major scale / Мажорна гама” in the new Release build and inspect the generic theory plus “Your variant / Твій варіант” card in en/uk. For C Standard the example is A♭ major; for Drop D it is C major. Check that the card's tuning/fret count/open strings/A4 match Settings, that shared help expands readably, and that selected steps still highlight the corresponding fretboard/TAB notes. Restore the actual guitar tuning setting before practice. The native automation bridge failed with “Sky Computer Use native pipe closed before response”, so this specific layout/interaction check remains open despite passing software tests and bundle validation.

Lesson position follow-up (2026-09-12), U05/U07 pending: in Major scale / Мажорна гама with C Standard, choose Fretboard position → From fret 7 / Позиція на грифі → Від 7-го ладу. Confirm A♭ major is unchanged and the ascending fingering starts on string 6/fret 8 and uses frets 7–10. Select steps/TAB/staff events, play/stop preview, open practice and inspect the selected-position label. Return/relaunch and check position and step restoration, then Original. Verify both languages, keyboard/VoiceOver, light/dark and minimum window. Change to corresponding Drop B♭: saved fret-7 region should become explicitly unavailable, with Original/other feasible choices; restore the actual C Standard setting before practice. Inspect frozen result/retry positions after a real attempt. The CUA bridge again failed with “Sky Computer Use native pipe closed before response”; passing model, synthetic transport, persistence and signed bundle checks do not establish this native or physical acceptance.


LD01–LD04 lesson activities (2026-09-12), U05/U07/U10 pending: in the new Release choose C Standard and 19 frets, open “Same notes, different positions / Ті самі ноти в різних ділянках”. Explore the single note at different starts, then select scale exploration → from fret 7 → fixed Original → fixed near-seven → back to exploration. Confirm independent choices, unchanged A♭ major pitches, correct source notes in fretboard/TAB/staff and preview stopping even between identical resolved exercises. Check the one-note staff fragment without invented trailing rests. Toggle the optional self-report and verify it is separate from audio scoring. Save/relaunch the lesson; change to Drop B♭ and check the demo's width-6 near-seven activity remains feasible (the starter major lesson's width-5 near-seven selection remains unavailable). Restore the actual physical tuning setting before any attempt. Perform each guided activity as a separate attempt, inspect bilingual history titles/context and retry its frozen notes. Check keyboard/VoiceOver, minimum window and both themes/languages. Two attempts to select the rebuilt Release through native CUA, including a kernel reset, returned `Computer Use server error -10005: timeoutReached`; no current native screen, launch or hardware success was established. Signed bundle/archive and model/synthetic/persistence checks do not close these gates.

Personal synchronization follow-up (2026-09-14): U03/U09/U10 must verify real signal preflight, all 16 repeated attacks, two-pass agreement, no clipping/click leakage, approximate score labels and no reuse after reconnect/relaunch. English/Ukrainian native layout, keyboard and VoiceOver remain pending. The synthetic runtime supplies analysis DTOs; it is not a live Scarlett/Bluetooth measurement.

## Audio-file AI coach — pending_user

- Select Codex/Claude in Settings and verify Record & analyze with a real guitar/interface: one take, clear channel 1, clicks on channel 2, complete practice and automatic advice without Terminal commands.
- Verify clean-input analysis and timing caveats with wired and Bluetooth output. The click track is a scheduled reference, not measured headphone sound.
- Verify en/uk layout, keyboard/VoiceOver, progress/cancel/error/retry/delete, imported file channel selection and reloaded saved feedback.
- Codex 0.154.0 returned a structured Ukrainian response through the signed sandbox/XPC path using a generated sine fixture. This is integration evidence, not guitar-coaching quality acceptance.
- Claude Code 2.1.236 launches but its current local OAuth session is expired and cannot refresh. Sign in again using `claude auth login`, then run the saved take. No account settings were changed by implementation.

### Input-level guidance follow-up (2026-09-16)

- With the real selected guitar input, vary physical gain while plucking: verify weak/working/high/overload hints in Audio setup, Timing synchronization and practice preflight. Lower gain before continuing after clipping. Confirm pauses/decay are not confused with a gain target.
- Verify English/Ukrainian layouts, light/dark contrast, scrolling of the synchronization sheet and VoiceOver reading of level/status/hint. No second capture stream is introduced.
- Working amplitude alone must not imply successful synchronization: selected-string pitch, clean/stable signal, timing repeatability and healthy clocks are still required.

### Practice score read-ahead (2026-09-16)

- Verify the full native scroll viewport (offline row images cannot establish this), 4/4 and 3/4 count-in, current-line visibility at row changes, manual reading with Follow playback disabled, zoom/resize, and repeated/pause/retry/subrange attempts.
- Compare the display with the real wired metronome; Bluetooth remains subject to unreported output delay. No sound recognition/scoring thresholds changed.
- Check EN/UK, light/dark, VoiceOver, keyboard event navigation across lazy rows and Reduce Motion. Native computer-use access timed out during implementation; these checks remain pending.

### Calibration first-pass rejection follow-up

- Repeat the reported first-pass failure with normal input level on the saved Scarlett input / separate headphone output. Inspect the new localized failure reason and pass diagnostics (counts, pitch quality, spread/window/drift and clock evidence); do not infer the old failure's cause from synthetic tests.
- Confirm the retained report is readable after input stops, clears on retry/cancel/route change, and English/Ukrainian plus VoiceOver expose the reason and values. Complete and explicitly apply two valid real passes before marking physical calibration successful.

Independent latency settings follow-up (2026-09-16): U03/U09/U10 now verify two independent settings. Output defaults to zero and can be measured with mouse/Space/Return without microphone access; no mandatory output-first pass. Instrument uses either zero or the explicitly applied output value and saves the remaining offset plus immutable output snapshot. Check focus/repeat suppression/VoiceOver, wired/Bluetooth audio timing, signed per-event deltas, last-note drain and all error markers. Failed/cancelled charts must remain readable after closing/reopening the sheet during the same app session. Output edits/reset must require instrument remeasurement. Original first-instrument-measurement failure remains undiagnosed on real hardware; capture a retained timeline and numeric diagnostics. Native interaction and hardware gates remain `pending_user`; synthetic DTO tests and offline renders are separate evidence.

Manual timing extension (2026-09-16): verify nonnegative dot/comma input (0–1000 ms) for both settings and rejection of negative corrections; output correction vs total-specification reference; no double compensation of reported hardware delay; retained failed charts; Manual setting labels in practice/results/history. Reapply restored manual values after relaunch/reconnect. Manual approximate scoring was explicitly selected by the user; no measured accuracy is claimed.

Initial timing-settings focus (2026-09-16): reopen the sheet and confirm that neither manual field starts editing. Tab and clicking should still reach both fields with their normal focus rings; audio-state refresh must not steal focus. Starting output measurement must still focus the tap control for Space/Return. Check keyboard navigation with Full Keyboard Access on/off and VoiceOver. The old autofocus was reproduced via native accessibility; the native connection closed after restarting the updated Release, so these follow-up checks remain pending.

Nonnegative settings (2026-09-16): confirm negative input shows an inline explanation and cannot Apply; measured negative offsets retain their timeline with Apply disabled. Previously saved negative output settings must show an explicit inactive/zero-fallback notice; negative instrument settings must not enable new scoring. Old history stays readable. Check total-specification values below reported output latency and combined values above 1000 ms.


### Lesson learning modes (2026-09-16) — pending_user

In the rebuilt Release, open “Before the first note / Перед першою нотою”, mark checklist items, answer the knowledge question incorrectly and retry correctly. Relaunch and verify the checkmarks/answer; language switching must preserve them. No fretboard/preview or microphone access is needed for theory. Open “Power chord: sound and silence / Павер-акорд: звук і тиша”, listen/loop the two-note example, play it on the guitar, then assess its criteria yourself. Confirm there is no automatically scored practice action for this chord. Change tuning/fret count and verify old self-report is not shown as current completion. Restore the physical guitar settings before playing.

Open “Hear pitch direction / Почути напрямок мелодії”, choose an audio output and listen. Answer diagrams and variant copy stay hidden. The full two-note example has no click/count-in; repeat, navigate away during playback and verify it stops. The answer/explanation must be localized and persist. Check keyboard/VoiceOver, minimum window size, en/uk and light/dark. Offline panel renders and synthetic output-runtime tests pass; actual interactive playback and VoiceOver have not been established by those checks.

## Full curriculum — first stage

- Inspect course/filters and reader at 900×620 in English/Ukrainian and light/dark; verify keyboard navigation, VoiceOver, step navigation, prerequisite and next-lesson links. Native Ukrainian/dark library/filter/search checks passed on 2026-09-16; reader automation was interrupted by an external SkyComputerUseService crash, not marked passed.
- Follow topics 1–16 on an actual guitar, including tuner/audio tool transitions; distinguish physical setup/self-checks from pitch/time grading. Confirm useful phrasing and practical tempo, especially while changing strings.
- Low-register grading now includes B Standard / Drop B♭ / Drop A at A4 = 440 Hz. On a real clean guitar, validate repeated A1–B1 attacks, sustained notes without spurious attacks, and the Standard/Drop lesson at 44.1/48 kHz. Try different pickup selections and picking strengths. Synthetic DSP/collector/assessment checks establish software behavior only; hardware accuracy remains pending. Recheck instrument timing after the analyzer version change.

### Rhythm duration extension — pending_user

- In English and Ukrainian, select the dotted/tied lesson and compare TAB with Staff in light/dark modes; read dotted symbols/tie continuations using keyboard and VoiceOver at the minimum window size.
- On a clean real guitar/interface, play the dotted and tied examples at 60 BPM. Verify one attack across the barline, full vs early release, wrong sustained pitch, muted/noisy input and calibration on/off. Record device/rate/channel/tuning/gain; synthetic fixtures are not this evidence.
- Select only the second tied bar, then pause during a tie and resume: confirm the range expands to a complete note and no hidden reattack is required.
- Check results, archived replay/retry and the optional Record & analyze path with a sustain lesson. Confirm uncertain capture never shows a successful duration score.

### Basic rhythm module and accents — pending_user

- Read and practice all eight basic-rhythm topics in English/Ukrainian, including sixteenth-note counting at 40–70 BPM and the original eight-bar study. Verify count-in changes to three beats for the 3/4 entry.
- Listen to the two accent examples: expected emphasis moves from 1/3 to 2/4 without moving the pulse. In scored practice, verify only the metronome is audible and dynamics remains explicitly self-assessed.
- Inspect `>` markers in normal TAB, compact practice TAB and staff in both themes and with VoiceOver; tied continuation must not announce a second accented attack. Offline images are not this native accessibility verification.

### First chord forms and root labels — pending_user

- Compare six-, five- and four-string minor forms and their major variants in all intended instrument presets. Check the named harmonic root against the actual chord; top voice/order must not rename it.
- Inspect × silent strings and suggested finger numbers, including their intentional removal when Drop adaptation changes a shape. Try each full chord acoustically, then its separated-note scored entry with the preceding string stopped.
- In both languages, move between chord preview and note practice; confirm full simultaneous chords are never presented as automatically graded. Real hand comfort and clarity are user evidence, not inferred from the pitch-class tests.

Strum reference follow-up: check the down/up arrows and spoken descriptions in English/Ukrainian, light/dark and minimum window width; compare synthetic down/up examples by ear, then play the authored five-string downstroke/three-string upstroke patterns. Confirm omitted strokes preserve hand motion without implying detector verification. Native VoiceOver, physical guitar comfort and perceived reference usefulness remain pending_user.

First-accompaniment follow-up: compare the intended and added-bass examples, practice both chord-change directions, check shared-finger small-barre comfort in short attempts, and play the sixteen-bar original verse/chorus at the written tempo. Confirm clear distinction between monophonic arpeggio scores and self-observed chord/barre performance. Inspect both languages, long-score navigation, light/dark and VoiceOver. These physical/native checks remain pending_user.

Open-bass anchor follow-up: in the Drop riff lesson compare the lowest open-string root in the actually selected tuning with the named root and TAB; compare Standard/Drop geometry only after physically retuning. Play the bass riff and separated-note entry with a clean input, then the ungraded full chord riff. In the expanded power-chord lesson verify the original full-chord example remains ungraded while the separated-note activity allows practice. Check the fifth quiz, octave example, both languages, VoiceOver and real fingering comfort. These checks remain pending_user.

Rock-rhythm/P.M. follow-up: inspect normal TAB, compact score and staff in both languages/themes with VoiceOver, especially low Drop A notes and adjacent eighths. Compare ringing and short synthetic references without assuming realistic palm-muted tone. On a real guitar try movement, fretting-hand and two-hand stops, pedal-note returns and the eight-bar study at 50 BPM. Confirm actual comfortable contact and clean rests, and the separation of graded unmuted pedal notes from ungraded muted/chord examples. Native/physical acceptance remains pending_user.

Fretboard-theory follow-up: read and practice topics 41–48 in both languages, checking note-name recall, unison versus octave, interval/degree explanations and major/minor comparison. In the map lesson compare Original/near-7/near-12 and learner-selected windows, retaining the exact sounding octave. On a real instrument verify comfortable long moves, actual use of the instructed string, current tuning labels and clean separated-note input. Keep instrument Settings matched to the guitar; the written Standard/Drop comparison does not require retuning. Native reader/keyboard/VoiceOver and educational/hardware acceptance remain pending_user.

Picking-direction follow-up: inspect down/up arrows and spoken direction in normal/compact TAB and staff, including ties, accents and P.M. combinations in both languages/themes. Play the alternate-picking and hand-synchronization lessons at 50 BPM; verify actual motion, clean string handovers, comfortable finger use and full rests yourself. The note score must not be presented as proof of physical pick direction or which hand caused a mistake. Native/hardware acceptance remains pending_user.

Bend/return follow-up — pending_user: on a clean real interface input, compare the separate fretted target with each half/whole-step bend at 40–60 BPM. Verify the physical starting fret remains comfortable in the actual tuning; do not treat pitch equality as proof of bending or repicking technique. Inspect base/rise/hold/release/returned phases and save/reopen the diagnostic graph; test a deliberately low target, overshoot, absent return and a quiet/poor-signal attempt. Confirm manual/personal calibration labels and perceived timing, both languages/themes, keyboard selection and spoken amount/return. Test ordinary practice and optional Record & analyze, ensuring bend-internal spectral observations are not described as proven extra pick attacks. Software synthetic/render evidence does not close these hardware/native checks.

Scale/position module follow-up — pending_user: read topics 57–64 in English/Ukrainian and compare every explanation with the visible TAB. Play the major/minor/pentatonic/blues examples and hear the tonic, changed third/sixth/seventh and passing blue note. On the actual six-string instrument compare the shared-note string choices, neighboring-region route and original/near-seven versions at the same tempo; confirm comfortable hand shifts rather than a forced six-fret stretch. Check quarter-note groups of three against the unchanged 4/4 metronome and explicit final rests. Inspect module order, combined filters/search, questions, saved self-observations and updated-version reading state with keyboard/VoiceOver and both themes. Pitch/attack scores do not verify string choice, physical technique, accents or expressive phrasing. Software instrument-matrix checks are not native/hardware acceptance.

Triplet/shuffle follow-up — pending_user: in both languages compare the straight-eighth/triplet example and the straight/shuffle pair with an unchanged quarter-note metronome. On a clean real instrument play at 40–90 BPM, including repeated pitches, string changes and the final two-slot rest. Confirm readable 3 brackets, quarter/eighth/rest notation, compact row spacing, cursor and saved results in both themes; verify keyboard and VoiceOver descriptions. Compare early/late short shuffle attacks without interpreting the fixed 2:1 target as a universal swing-quality score. Only synthetic capture and offline render evidence is currently established.

Rhythm-phrasing follow-up — pending_user: play topics 67/69/70 with the actual tuning and clean interface input. Compare offbeat attacks with the unchanged click, hold through the written downbeat/barline, then inspect sustain coverage separately from attack timing. At 40–70 BPM compare ordinary/reverse gallop without turning the short pair into triplets; check physical alternate picking and open-bass comfort yourself. Count the three-bar accent cycle, its shifted version and four motif starting points through the written silences. Inspect long-rest fragments, ties, arrows, accents and read-ahead in both languages/themes with keyboard/VoiceOver. Audio scores do not establish accent strength, palm placement or pick direction. Software matrix tests do not close native/hardware acceptance.
